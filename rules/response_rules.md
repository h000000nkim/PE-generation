## 응답 규칙
1. 불필요한 서론, 감사 표현, 설명 없이 결과만 출력한다.
2. STEP 2(파일 분석) → STEP 3(사람 분석) → STEP 4(과제 분석) 순서로 출력한다.
3. 분석 출력 후 바로 STEP 5(작성)에 들어간다.
4. 결과물은 `과목_과제제목_이름.확장자` 형식의 개별 파일 + result.json (구조화 데이터)으로 저장한다. **result.md, result.docx 같은 임시 파일명 금지.**

## result.json 형식
과제가 여러 산출물을 요구할 수 있다 (예: PPT + 구글폼 + 발표대본).
각 산출물을 `outputs` 배열에 넣어 저장할 것:
```json
{
  "outputs": [
    {
      "label": "산출물 이름 (예: 보고서, 발표대본, 구글폼 응답 등)",
      "type": "docx | hwpx | pptx | form",
      "file": "파일명",
      "canva_edit_url": "Canva 편집 URL (pptx인 경우만)",
      "canva_view_url": "Canva 보기 URL (pptx인 경우만)",
      "pptx_download_url": "PPTX 다운로드 URL (pptx인 경우만)"
    }
  ]
}
```
- 산출물 하나당 배열 요소 하나
- type이 docx이면 Word MCP로 .docx 파일 생성 후 file에 파일명 기록
- type이 hwpx이면 HWPX MCP로 .hwpx 파일 생성 후 file에 파일명 기록
- type이 pptx이면 Canva로 생성 후 URL 기록
- type이 form이면 구글폼 응답을 .docx 또는 .hwpx 파일로 저장
- **자필(유인물) 형식도 반드시 docx 또는 hwpx로 저장** — 학생이 옮겨 쓸 원본용
- 모든 산출물의 텍스트 원문은 반드시 개별 파일로도 저장할 것

## 파일명 규칙 ⚠️ 반드시 준수
`과목_과제제목_이름.확장자` 형식으로 저장한다.
**`result.docx`, `result.hwpx` 같은 임시 파일명은 절대 사용 금지.**

- 산출물이 1개인 경우: `과목_과제제목_이름.확장자`
  - 예) `진로_왜세상의절반은굶주리는가_김하율.docx`
  - 예) `영어1_KlaraAndTheSun_김동우.docx`
- 산출물이 여러 개인 경우: 과제제목과 이름 사이에 `_파일종류_`를 넣는다
  - 예) `음악감상과비평_불꽃이들려주는음악의밤_구글폼_남수민.docx`
  - 예) `음악감상과비평_불꽃이들려주는음악의밤_대본_남수민.docx`
  - 예) `음악감상과비평_불꽃이들려주는음악의밤_ppt_남수민.pptx`
  - 예) `음악감상과비평_불꽃이들려주는음악의밤_예상질문_남수민.docx`
- 과목명은 CLAUDE.md의 `과목(mGaa)` 값을 사용 (공백 제거)
- 과제제목은 핵심 주제를 간결하게 (공백 제거, 특수문자 제거)
- 이름은 CLAUDE.md의 `이름` 값 그대로 사용
- 확장자는 제출 형식에 맞게: `.docx` (워드), `.hwpx` (한글), `.pptx` (PPT)
